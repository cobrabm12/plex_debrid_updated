# rclone remote used by the in-Docker "rclone" service to mount the Zurg WebDAV.
# The installer copies this to rclone/rclone.conf. Inside the compose network the
# zurg container is reachable as the host "zurg".
[zurg]
type = webdav
url = http://zurg:9999/dav/
vendor = other
pacer_min_sleep = 0
